{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Package",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            // 关键修改：使用工作区根目录作为程序入口（包含 go.mod 和 Go 代码的目录）
            "program": "${workspaceFolder}", 
            // 可选：如果需要传递命令行参数，可添加 args
            "args": []
        }
    ]
}